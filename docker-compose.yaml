version: '3.8'
services:
  proxy:
    restart: always
    build: .
    ports:
      - '8080:80'
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./servers:/etc/nginx/servers
      - ./default:/app/default